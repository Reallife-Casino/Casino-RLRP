'use client';import {useState} from 'react';
export default function DepositForm({fivemId}:{fivemId:string}){const [msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setMsg('');const f=new FormData(e.currentTarget);const r=await fetch('/api/deposits',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(f.entries()))});const j=await r.json();setMsg(r.ok?'Stortingsmelding verzonden. Een beheerder moet hem nog goedkeuren.':j.error);if(r.ok)e.currentTarget.reset()}return <form className="form" onSubmit={submit}>{msg&&<div className={msg.startsWith('Stort')?'success':'error'}>{msg}</div>}<label className="label">FiveM ID<input className="input" value={fivemId} readOnly/></label><label className="label">Bedrag RP<input className="input" name="amount" inputMode="numeric" required placeholder="50000"/></label><label className="label">Opmerking (optioneel)<textarea className="input" name="note" rows={3}/></label><button className="btn gold">STORTING MELDEN</button></form>}
