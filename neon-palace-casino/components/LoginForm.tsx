'use client';
import { useState } from 'react';import { useRouter } from 'next/navigation';
export default function LoginForm(){const [error,setError]=useState('');const [busy,setBusy]=useState(false);const r=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);const res=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(f.entries()))});const j=await res.json();setBusy(false);if(!res.ok){setError(j.error||'Inloggen mislukt');return;}r.push('/dashboard');r.refresh();}return <form className="form" onSubmit={submit}>{error&&<div className="error">{error}</div>}<label className="label">Accountnaam<input className="input" name="username" required/></label><label className="label">Wachtwoord<input className="input" type="password" name="password" required/></label><button className="btn primary" disabled={busy}>{busy?'Inloggen…':'INLOGGEN'}</button></form>}
