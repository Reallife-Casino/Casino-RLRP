'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function WithdrawForm(){const [msg,setMsg]=useState('');const r=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const res=await fetch('/api/withdrawals',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(f.entries()))});const j=await res.json();setMsg(res.ok?'Uitbetalingsaanvraag aangemaakt. Het bedrag is nu gereserveerd.':j.error);if(res.ok){e.currentTarget.reset();r.refresh()}}return <form className="form" onSubmit={submit}>{msg&&<div className={msg.startsWith('Uitbetal')?'success':'error'}>{msg}</div>}<label className="label">Bedrag RP<input className="input" name="amount" required inputMode="numeric" placeholder="25000"/></label><button className="btn gold">UITBETALING AANVRAGEN</button></form>}
